# How to Run

Tech used
```
-  Spring boot
-  H2 database (in memory)
-  Liquibase
-  Java 8
```
1. Clone the project, and in the home directory run command
```
$ ./mvnw spring-boot:run
```

2. Open browser and go to http://localhost:8080/swagger-ui.html